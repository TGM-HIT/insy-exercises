# Projektname trennt Container und Volumes von anderen Übungen.
COMPOSE_PROJECT_NAME=insy-cms-team01
WP_PORT=8085
# Zwei unterschiedliche lokale Übungspasswörter eintragen (z.B. je 24 Zeichen).
# Für unkomplizierte .env-Verarbeitung Buchstaben und Ziffern verwenden.
DB_PASSWORD=
DB_ROOT_PASSWORD=
