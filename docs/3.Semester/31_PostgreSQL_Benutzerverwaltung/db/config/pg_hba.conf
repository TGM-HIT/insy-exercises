# AUSGANGSZUSTAND: TLS ist aktiviert, aber noch nicht verpflichtend.
# Aufgabe 4: TCP-Regeln bearbeiten; die erste passende Regel entscheidet.
# Diese Datei wird als Teil des config-Verzeichnisses read-only eingebunden.
# Auf dem Host bearbeiten und danach SELECT pg_reload_conf() ausfuehren.

# Lokaler Einrichtungs- und Wiederherstellungszugang im DB-Container.
local   all   postgres                          trust
local   all   all                               scram-sha-256

# Absichtlich noch keine IP-Sperre und keine TLS-Pflicht.
host    all   all   0.0.0.0/0                    scram-sha-256
host    all   all   ::/0                         scram-sha-256
